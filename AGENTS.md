# AGENTS.md

## 1. 프로젝트 개요

이 저장소는 Unity 6 계열과 C#으로 제작하는 **2D 횡스크롤 싱글 액션 RPG
포트폴리오 프로젝트**다.

-   플랫폼: Windows PC
-   엔진: Unity 6
-   렌더링: Universal 2D
-   언어: C#
-   장르: 2D 픽셀 횡스크롤 싱글 액션 RPG
-   목표: 6개월 수직 슬라이스 완성, 이후 최대 12개월까지 확장
-   1차 완성 기준: 전사 캐릭터로 시작부터 엔딩까지 플레이 가능
-   최종 직업: 전사 / 궁수 / 마법사
-   캐릭터 슬롯: 최대 5개
-   전투 방향: 단순하고 명확한 기본 공격 + 직업별 스킬 성장

이 프로젝트의 개발자는 Unreal Engine 및 C++ 경험이 있지만 Unity와 C#은
처음 사용한다.

------------------------------------------------------------------------

## 2. Codex 작업 원칙

### 가장 중요한 규칙

1.  기존에 정상 작동하는 기능을 불필요하게 다시 작성하지 않는다.
2.  요청받은 범위 밖의 대규모 리팩터링을 임의로 수행하지 않는다.
3.  한 번에 많은 시스템을 구현하기보다 작은 단위로 구현하고 검증한다.
4.  코드 변경 전 현재 구조와 관련 스크립트를 먼저 확인한다.
5.  기존 Inspector 참조, Prefab, Animator, Input Action과의 연결이
    깨지지 않도록 한다.
6.  Unity Editor에서 별도의 설정이 필요한 경우 코드 수정과 함께 정확한
    Editor 작업 절차를 설명한다.
7.  새로운 패키지나 외부 라이브러리는 꼭 필요한 경우가 아니면 추가하지
    않는다.
8.  현재 프로토타입 단계에서는 과도한 추상화와 오버엔지니어링을 피한다.
9.  단기 편의를 위해 모든 기능을 하나의 MonoBehaviour에 몰아넣지 않는다.
10. 작업 후 무엇을 변경했는지와 Unity에서 무엇을 확인해야 하는지
    요약한다.

------------------------------------------------------------------------

## 3. 설명 방식

개발자는 C++ 경험이 있으므로 프로그래밍 기초 자체를 장황하게 설명할
필요는 없다.

다만 Unity와 C# 고유 개념은 처음 사용하는 사람을 기준으로 설명한다.

필요한 경우 Unreal/C++과 비교한다.

예:

-   `MonoBehaviour` ↔ Unreal의 Actor/Component 기반 수명주기와 비교
-   `Awake`, `Start`, `Update`, `FixedUpdate` ↔ Unreal의 초기화/Tick
    개념과 비교
-   `ScriptableObject` ↔ 데이터 에셋과 유사한 역할
-   Inspector 직렬화 ↔ 에디터 노출 프로퍼티와 비교
-   Prefab ↔ 재사용 가능한 GameObject 템플릿 개념
-   `Rigidbody2D` ↔ 물리 컴포넌트
-   `LayerMask` ↔ Collision Channel/Mask와 유사한 용도

코드를 수정할 때는 가능하면 다음을 알려준다.

-   왜 수정하는지
-   어느 파일을 수정했는지
-   핵심 코드가 어떤 역할을 하는지
-   Unity Inspector에서 추가로 연결할 것이 있는지
-   테스트 방법

------------------------------------------------------------------------

## 4. 현재 사용 기술

가능하면 현재 프로젝트에서 이미 선택한 기술을 유지한다.

-   Unity 6
-   C#
-   Universal 2D
-   Rigidbody2D
-   Collider2D
-   Tilemap
-   Animator
-   Input System
-   ScriptableObject
-   UGUI / TextMeshPro
-   JSON 또는 Unity 직렬화
-   Git / GitHub

기존 Input System을 사용하고 있으므로 특별한 이유 없이 Legacy Input
방식으로 변경하지 않는다.

------------------------------------------------------------------------

## 5. 플레이어 기본 조작

기본 조작 방향은 다음과 같다.

-   좌우 이동: 방향키 ← →
-   아래 입력: 방향키 ↓
-   점프: Alt
-   기본 공격: Ctrl
-   상호작용/포털: Space
-   아이템 줍기: Z
-   스킬/물약 퀵슬롯: Q W E R A S D F

방향키 이동은 고정한다.

점프, 공격, 스킬, 물약 등의 키는 향후 키 설정에서 변경할 수 있도록
설계한다.

현재 Input System의 Action Map은 `Player`이며 주요 Action은 다음과 같다.

-   `Move`: Value / Vector2
-   `Jump`: Button
-   `Attack`: Button

------------------------------------------------------------------------

## 6. 현재 Player 구조

현재 기본 구조를 임의로 변경하지 않는다.

``` text
Player
├─ Visual
├─ AttackPoint
└─ GroundCheck
```

역할:

-   `Player`: Rigidbody2D, Collider2D 및 게임플레이 로직의 기준
-   `Visual`: SpriteRenderer와 Animator 등 시각 요소
-   `AttackPoint`: 근접 공격 판정 위치
-   `GroundCheck`: 지면 판정 위치

캐릭터 방향 전환은 가능하면 `Player` 전체가 아니라 `Visual`의 X Scale을
반전한다.

그 이유는 Player 전체를 뒤집을 경우 Collider, GroundCheck 등 게임플레이
요소까지 영향을 받을 수 있기 때문이다.

`AttackPoint`는 게임플레이 판정 요소이므로 Visual 애니메이션과
독립적으로 관리한다.

------------------------------------------------------------------------

## 7. 플레이어 이동 규칙

현재 플레이어는 Rigidbody2D 기반으로 이동한다.

-   좌우 이동
-   점프
-   Alt 키를 누르고 있을 때 착지 후 다시 점프 가능
-   통과형 발판
-   ↓ + Alt를 통한 아래 점프
-   공중 상승 중 GroundCheck가 잘못 지면으로 판정하지 않도록 수직 속도도
    고려

물리 처리는 가능한 한 `FixedUpdate`와 Rigidbody2D를 사용한다.

Transform을 직접 이동시켜 Rigidbody2D 물리와 충돌하는 방식을 특별한 이유
없이 사용하지 않는다.

------------------------------------------------------------------------

## 8. 통과형 발판

통과형 발판은 다음 구조를 사용한다.

-   BoxCollider2D
-   PlatformEffector2D
-   `Used By Effector`
-   `Use One Way`
-   별도 `Platform` Layer

아래 점프는 전체 Layer 충돌을 끄는 방식보다 **플레이어 Collider와 현재
발판 Collider 사이의 충돌만 일시적으로 무시하는 방식**을 우선한다.

다른 발판이나 다른 오브젝트의 충돌까지 동시에 비활성화하지 않는다.

------------------------------------------------------------------------

## 9. 카메라

현재 카메라는 플레이어를 따라가는 2D 카메라다.

-   SmoothDamp 기반 추적
-   맵 경계 Clamp
-   수직 Dead Zone
-   점프할 때 카메라가 매번 즉시 위아래로 따라가지 않도록 구성

카메라 관련 코드를 수정할 때는 기존 Dead Zone과 맵 경계 기능을 보존한다.

------------------------------------------------------------------------

## 10. 애니메이션

현재 전사 캐릭터 Animator를 사용한다.

주요 애니메이션:

-   Idle
-   Run
-   Jump
-   Fall
-   Attack
-   Death 등

주요 Animator Parameter:

-   `Speed`: Float
-   `JumpSpeed`: Float
-   `Attack`: Trigger

공격은 Animation Event를 사용해 **애니메이션 시점과 실제 공격 판정
시점을 연결**한다.

현재 이벤트 중 중요한 함수:

-   `HitAttack()`: 실제 공격 판정 발생
-   `EndAttack()`: 공격 상태 종료

Animator가 `Visual` 자식에 있기 때문에 Animation Event 수신용 컴포넌트
역시 Visual에 위치할 수 있다.

Animation Event가 호출하는 함수와 실제 게임플레이 코드는 필요하면 부모
Player의 컴포넌트로 전달한다.

------------------------------------------------------------------------

## 11. 전투 구조

현재 전투 흐름의 기본 구조는 다음과 같다.

``` text
Input System
    ↓
PlayerController
    ↓
Animator
    ↓
Animation Event
    ↓
PlayerCombat
    ↓
EnemyHealth
    ↓
DamagePopup
```

책임을 가능한 한 유지한다.

### PlayerController

-   입력
-   이동
-   점프
-   방향
-   플레이어 상태와 애니메이션 연결

### PlayerCombat

-   공격 범위
-   공격 대상 탐색
-   공격 데미지 전달

### EnemyHealth

-   적 HP
-   피해 처리
-   사망 처리
-   피해 관련 이벤트 호출

### DamagePopup

-   데미지 숫자 표시
-   위로 이동
-   일정 시간 후 제거

`PlayerController` 안에 적 HP 처리나 데미지 숫자 생성 로직을 직접 넣지
않는다.

------------------------------------------------------------------------

## 12. 공격 판정

현재 근접 공격은 `AttackPoint`를 중심으로 `Physics2D.OverlapCircleAll`을
사용한다.

적은 `Enemy` Layer로 구분한다.

공격 판정은 공격 버튼을 누른 순간 무조건 발생시키는 것이 아니라,
가능하면 공격 애니메이션의 실제 타격 프레임에 있는 `HitAttack` Animation
Event에서 실행한다.

애니메이션과 실제 판정 로직을 분리한다.

------------------------------------------------------------------------

## 13. Damage Popup

현재 데미지 숫자는 월드 위치를 따라야 하므로 각 DamagePopup Prefab
자체가 **World Space Canvas**다.

구조:

``` text
DamagePopup
└─ DamageText
```

-   `DamagePopup`: World Space Canvas
-   `DamageText`: TextMeshProUGUI
-   적의 `DamagePopupPoint` 위치에서 생성
-   생성 후 적의 자식으로 유지하지 않음
-   맞은 위치에서 독립적으로 위로 이동
-   일정 시간이 지나면 Destroy

특별한 이유 없이 Screen Space Overlay 기반 데미지 숫자로 변경하지
않는다.

------------------------------------------------------------------------

## 14. 적 시스템 개발 방향

현재 첫 번째 테스트 적부터 다음 전투 루프를 완성한다.

``` text
플레이어 공격
→ 적 피격
→ HP 감소
→ 데미지 숫자
→ HP 0
→ 사망 상태
→ Death 애니메이션
→ 충돌/공격 대상 비활성화
→ 필요 시 일정 시간 후 제거
```

향후 Enemy AI는 상태 머신 기반으로 확장할 수 있도록 한다.

예상 상태:

``` text
Idle
Patrol
Chase
Attack
Hit
Dead
```

하지만 현재 필요한 상태보다 지나치게 큰 범용 FSM 프레임워크를 먼저
만들지는 않는다.

------------------------------------------------------------------------

## 15. 데이터 설계 방향

게임 규모가 커지면 다음 데이터는 ScriptableObject와 ID 기반으로
분리한다.

-   직업
-   스킬
-   장비
-   몬스터
-   퀘스트

UI와 저장 데이터가 Scene의 GameObject 직접 참조에 과도하게 의존하지
않도록 한다.

직업이 늘어날 때 다음과 같은 긴 분기 구조를 피한다.

``` csharp
if (job == Warrior)
{
}
else if (job == Archer)
{
}
else if (job == Mage)
{
}
```

대신 데이터 주도 설계, 공통 인터페이스, 역할별 클래스를 고려한다.

단, 아직 실제로 필요하지 않은 시스템을 미리 과도하게 추상화하지 않는다.

------------------------------------------------------------------------

## 16. 예정 주요 클래스

장기적으로 다음 책임 분리를 목표로 한다.

``` text
PlayerController
PlayerCombat
SkillController
CharacterData
JobData
EquipmentData
Inventory
AccountStorage
EnemyAI
QuestManager
SaveManager
```

현재 존재하지 않는 클래스를 요청 없이 한꺼번에 생성하지 않는다.

필요한 개발 단계가 되었을 때 하나씩 추가한다.

------------------------------------------------------------------------

## 17. 저장 구조 방향

향후 저장 시스템에서는 계정 데이터와 캐릭터별 데이터를 분리한다.

계정 데이터 예:

-   캐릭터 슬롯
-   공용 창고
-   환경 설정
-   키 설정

캐릭터 데이터 예:

-   이름
-   직업
-   레벨
-   장비
-   인벤토리
-   스킬
-   퀘스트 진행
-   위치
-   퀵슬롯

캐릭터를 삭제해도 공용 창고가 함께 삭제되는 구조를 만들지 않는다.

------------------------------------------------------------------------

## 18. 개발 우선순위

현재 단계에서는 완성된 본편보다 기술 검증과 기본 전투 루프가 우선이다.

대략적인 우선순위:

``` text
이동/점프
→ 통과형 발판
→ 카메라
→ 전사 기본 공격
→ 몬스터 피격
→ 데미지 숫자
→ 몬스터 사망
→ 피격 연출/히트 스톱/화면 흔들림
→ 테스트 전투 루프
→ 이후 스킬/AI/장비/인벤토리/퀘스트/저장
```

새로운 큰 시스템을 추가하기 전에 현재 단계의 기능이 정상 동작하는지 먼저
확인한다.

------------------------------------------------------------------------

## 19. 포트폴리오 관점의 코드 품질

이 프로젝트는 단순 학습 프로젝트가 아니라 게임 프로그래머 포트폴리오
제출을 목표로 한다.

따라서 다음을 중요하게 본다.

-   책임이 구분된 클래스 구조
-   읽기 쉬운 변수명과 함수명
-   불필요한 public 필드 최소화
-   필요한 Inspector 노출은 `[SerializeField]` 고려
-   NullReference 가능성 점검
-   불필요한 `Find` 반복 호출 방지
-   Update에서 불필요한 할당/탐색 최소화
-   물리와 애니메이션의 역할 분리
-   데이터와 GameObject 로직의 과도한 결합 방지
-   상태 머신 기반 Enemy AI
-   저장 데이터 버전 관리 고려
-   오브젝트 풀링이 필요한 시점에는 프로파일링 후 적용

단, 포트폴리오용이라는 이유로 작은 기능까지 복잡한 디자인 패턴으로
감싸지 않는다.

------------------------------------------------------------------------

## 20. 변경 시 주의사항

Codex가 기존 코드를 수정할 때 다음 절차를 따른다.

1.  관련 스크립트와 구조를 먼저 확인한다.
2.  기존 동작을 파악한다.
3.  변경 범위를 최소화한다.
4.  컴파일 오류 가능성을 확인한다.
5.  Unity 직렬화 필드 이름 변경 시 기존 Inspector 참조 손실 가능성을
    고려한다.
6.  Animator Parameter 이름을 변경할 경우 Animator Controller와 코드
    양쪽을 확인한다.
7.  Input Action 이름을 변경할 경우 생성된 Input System C# 클래스와
    호출부를 확인한다.
8.  Prefab/Scene 변경이 필요한 경우 코드만 수정하고 끝내지 말고 필요한
    Editor 작업을 알려준다.
9.  변경 후 테스트 절차를 제시한다.
10. 기존 기능이 깨질 가능성이 있으면 명확히 알린다.

------------------------------------------------------------------------

## 21. 현재 단계

현재는 **1주차 프로토타입 단계**다.

이미 구현 또는 검증된 주요 기능:

-   Unity 프로젝트 기본 구성
-   Input System
-   좌우 이동
-   점프
-   통과형 발판
-   아래 점프
-   Ground Check
-   카메라 추적
-   카메라 수직 Dead Zone
-   전사 Sprite/Animator
-   Idle / Run / Jump / Fall
-   좌우 방향 전환
-   기본 공격 애니메이션
-   Animation Event
-   AttackPoint 기반 공격 판정
-   Enemy HP 감소
-   World Space 데미지 숫자

현재 다음 우선 작업은 **몬스터 HP가 0이 되었을 때의 사망 루프
완성**이다.

현재 정상 작동하는 위 기능들을 대규모로 다시 작성하지 말고, 이 상태를
기반으로 다음 기능을 추가한다.
